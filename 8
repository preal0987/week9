clear; clc;
x = -2*pi:0.1:2*pi;
y = -2*pi:0.1:2*pi;
[X, Y] = meshgrid(x, y);
Z = sin(X).*cos(Y);
subplot(1,2,1)
mesh(X, Y, Z)
title('mesh graph')
xlabel('x')
ylabel('y')
zlabel('z')
grid on
subplot(1,2,2)
contour(X, Y, Z)
title('contour graph')
xlabel('x')
ylabel('y')
grid on
